#ifndef TYPEDEFS_H
#define TYPEDEFS_H

#include <string>
#include <vector>
#include <map>

using namespace std;

// Typedefs
typedef vector<string> vectorstr;
typedef vector<long> vectorlong;
typedef map<string, long> map_sl;
typedef multimap<string, long> mmap_sl;
typedef multimap<string, string> mmap_ss;

#endif

